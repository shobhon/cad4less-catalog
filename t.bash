# Ensure you are in the project root and on main
pwd
git branch -vv

# Stage everything (all files, all subfolders)
git add -A

# Commit them
git commit -m "Add full cad4less-catalog application"

# Push to GitHub
git push -u origin main